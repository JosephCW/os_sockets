#include <sys/socket.h>
#include <sys/types.h>


// Need a socket, ip address, and port.

//int socket (int domain, int type, int protocol)
// AF_INET for ipv4 as domain
// SOCK_STREAM is TCP, SOCK_DGRAM is UDP
// Set protocol flag to zero.
int main(int argc, char*argv[]) {
// Server
  // Bind, Listen, and Accept
// Client
  // Connect to server
// Both
  // Socket, Send, Write, Recieve

}
